apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-initdb-config
data:
  init.sql: |
    CREATE DATABASE IF NOT EXISTS test;
    USE test;

    CREATE TABLE personas (id INT NOT NULL AUTO_INCREMENT, disease VARCHAR(255) NOT NULL, job VARCHAR(255) NOT NULL, age INT NOT NULL, salary INT NOT NULL, lat FLOAT NOT NULL, lon FLOAT NOT NULL, name VARCHAR(255) NOT NULL, temperature FLOAT NOT NULL, pulse INT NOT NULL, PRIMARY KEY(id));
    CREATE TABLE usuarios (id INT NOT NULL AUTO_INCREMENT, email VARCHAR(255) NOT NULL, password VARCHAR(255) NOT NULL, salt VARCHAR(255) NOT NULL, type VARCHAR(255) NOT NULL, PRIMARY KEY (id));
    CREATE TABLE personas (id INT NOT NULL AUTO_INCREMENT, disease VARCHAR(255) NOT NULL, job VARCHAR(255) NOT NULL, age INT NOT NULL, salary INT NOT NULL, lat FLOAT NOT NULL, lon FLOAT NOT NULL, name VARCHAR(255) NOT NULL, temperature FLOAT NOT NULL, pulse INT NOT NULL, PRIMARY KEY(id));
    CREATE TABLE usuarios (id INT NOT NULL AUTO_INCREMENT, email VARCHAR(255) NOT NULL, password VARCHAR(255) NOT NULL, salt VARCHAR(255) NOT NULL, type VARCHAR(255) NOT NULL, PRIMARY KEY (id));
    CREATE TABLE personas (id INT NOT NULL AUTO_INCREMENT, disease VARCHAR(255) NOT NULL, job VARCHAR(255) NOT NULL, age INT NOT NULL, salary INT NOT NULL, lat FLOAT NOT NULL, lon FLOAT NOT NULL, name VARCHAR(255) NOT NULL, temperature FLOAT NOT NULL, pulse INT NOT NULL, PRIMARY KEY(id));
    CREATE TABLE usuarios (id INT NOT NULL AUTO_INCREMENT, email VARCHAR(255) NOT NULL, password VARCHAR(255) NOT NULL, salt VARCHAR(255) NOT NULL, type VARCHAR(255) NOT NULL, PRIMARY KEY (id));
    CREATE TABLE personas (id INT NOT NULL AUTO_INCREMENT, disease VARCHAR(255) NOT NULL, job VARCHAR(255) NOT NULL, age INT NOT NULL, salary INT NOT NULL, lat FLOAT NOT NULL, lon FLOAT NOT NULL, name VARCHAR(255) NOT NULL, temperature FLOAT NOT NULL, pulse INT NOT NULL, PRIMARY KEY(id));
    CREATE TABLE usuarios (id INT NOT NULL AUTO_INCREMENT, email VARCHAR(255) NOT NULL, password VARCHAR(255) NOT NULL, salt VARCHAR(255) NOT NULL, type VARCHAR(255) NOT NULL, PRIMARY KEY (id));
    CREATE TABLE personas (id INT NOT NULL AUTO_INCREMENT, disease VARCHAR(255) NOT NULL, job VARCHAR(255) NOT NULL, age INT NOT NULL, salary INT NOT NULL, lat FLOAT NOT NULL, lon FLOAT NOT NULL, name VARCHAR(255) NOT NULL, temperature FLOAT NOT NULL, pulse INT NOT NULL, PRIMARY KEY(id));
    CREATE TABLE usuarios (id INT NOT NULL AUTO_INCREMENT, email VARCHAR(255) NOT NULL, password VARCHAR(255) NOT NULL, salt VARCHAR(255) NOT NULL, type VARCHAR(255) NOT NULL, PRIMARY KEY (id));
    CREATE TABLE personas (id INT NOT NULL AUTO_INCREMENT, disease VARCHAR(255) NOT NULL, job VARCHAR(255) NOT NULL, age INT NOT NULL, salary INT NOT NULL, lat FLOAT NOT NULL, lon FLOAT NOT NULL, name VARCHAR(255) NOT NULL, temperature FLOAT NOT NULL, pulse INT NOT NULL, PRIMARY KEY(id));
    CREATE TABLE usuarios (id INT NOT NULL AUTO_INCREMENT, email VARCHAR(255) NOT NULL, password VARCHAR(255) NOT NULL, salt VARCHAR(255) NOT NULL, type VARCHAR(255) NOT NULL, PRIMARY KEY (id));
    CREATE TABLE personas (id INT NOT NULL AUTO_INCREMENT, disease VARCHAR(255) NOT NULL, job VARCHAR(255) NOT NULL, age INT NOT NULL, salary INT NOT NULL, lat FLOAT NOT NULL, lon FLOAT NOT NULL, name VARCHAR(255) NOT NULL, temperature FLOAT NOT NULL, pulse INT NOT NULL, PRIMARY KEY(id));
    CREATE TABLE usuarios (id INT NOT NULL AUTO_INCREMENT, email VARCHAR(255) NOT NULL, password VARCHAR(255) NOT NULL, salt VARCHAR(255) NOT NULL, type VARCHAR(255) NOT NULL, PRIMARY KEY (id));
    CREATE TABLE personas (id INT NOT NULL AUTO_INCREMENT, disease VARCHAR(255) NOT NULL, job VARCHAR(255) NOT NULL, age INT NOT NULL, salary INT NOT NULL, lat FLOAT NOT NULL, lon FLOAT NOT NULL, name VARCHAR(255) NOT NULL, temperature FLOAT NOT NULL, pulse INT NOT NULL, PRIMARY KEY(id));
    CREATE TABLE usuarios (id INT NOT NULL AUTO_INCREMENT, email VARCHAR(255) NOT NULL, password VARCHAR(255) NOT NULL, salt VARCHAR(255) NOT NULL, type VARCHAR(255) NOT NULL, PRIMARY KEY (id));
    CREATE TABLE personas (id INT NOT NULL AUTO_INCREMENT, disease VARCHAR(255) NOT NULL, job VARCHAR(255) NOT NULL, age INT NOT NULL, salary INT NOT NULL, lat FLOAT NOT NULL, lon FLOAT NOT NULL, name VARCHAR(255) NOT NULL, temperature FLOAT NOT NULL, pulse INT NOT NULL, PRIMARY KEY(id));
    CREATE TABLE usuarios (id INT NOT NULL AUTO_INCREMENT, email VARCHAR(255) NOT NULL, password VARCHAR(255) NOT NULL, salt VARCHAR(255) NOT NULL, type VARCHAR(255) NOT NULL, PRIMARY KEY (id));
    CREATE TABLE personas (id INT NOT NULL AUTO_INCREMENT, disease VARCHAR(255) NOT NULL, job VARCHAR(255) NOT NULL, age INT NOT NULL, salary INT NOT NULL, lat FLOAT NOT NULL, lon FLOAT NOT NULL, name VARCHAR(255) NOT NULL, temperature FLOAT NOT NULL, pulse INT NOT NULL, PRIMARY KEY(id));
    CREATE TABLE usuarios (id INT NOT NULL AUTO_INCREMENT, email VARCHAR(255) NOT NULL, password VARCHAR(255) NOT NULL, salt VARCHAR(255) NOT NULL, type VARCHAR(255) NOT NULL, PRIMARY KEY (id));
    CREATE TABLE personas (id INT NOT NULL AUTO_INCREMENT, disease VARCHAR(255) NOT NULL, job VARCHAR(255) NOT NULL, age INT NOT NULL, salary INT NOT NULL, lat FLOAT NOT NULL, lon FLOAT NOT NULL, name VARCHAR(255) NOT NULL, temperature FLOAT NOT NULL, pulse INT NOT NULL, PRIMARY KEY(id));
    CREATE TABLE usuarios (id INT NOT NULL AUTO_INCREMENT, email VARCHAR(255) NOT NULL, password VARCHAR(255) NOT NULL, salt VARCHAR(255) NOT NULL, type VARCHAR(255) NOT NULL, PRIMARY KEY (id));
